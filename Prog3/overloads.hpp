#ifndef OVERLOADS_HPP
#define OVERLOADS_HPP

 /** overloads.hpp
  * 
  * Justin Greever
  * CS202 - Program 3
  * 2/18/2021
  * 
  * The purpose of this file is to hold the functions for the
  * operator overloads.
  * Using https://en.cppreference.com/w/cpp/language/operators
  * as a reference for Operator Overloading.
  * 
  * Overloads planned (+/- a few more later):
  *   bool &operator==(const T&);
  *   bool &operator!=(const T&);
  *   bool &operator<(const T&);
  *   bool &operator>(const T&);
  *   T& operator+(const T&, const T&);
  *   T& operator[](const T&) const;
  *   T& operator=(const T&);
  *   T& operator+=(const T&);
  */

 #include "device.hpp"


#endif