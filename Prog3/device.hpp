#include <iostream>
#include <cstring>
#include <cctype>
using std::cin;
using std::cout;
using std::endl;

class device
{
    public:

    private:

    protected:
        char *device_type;
        device *a_device;
};